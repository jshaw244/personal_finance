# /scripts/ - Utility and One-Off Scripts

- Install-PlaidProfileShortcuts.py — installs Plaid convenience functions into your PowerShell profile
- trigger_webhook.py — manually trigger a webhook event in sandbox
- load_env.ps1 — load environment variables into session
- plaid_profile_snippet.ps1 — configure Plaid dev profile shortcuts