# 🧩 SANDBOX LIFECYCLE CHEAT SHEET

Everything below assumes your current setup (C:\DATA\personal_finance\ — Flask + ngrok + SQLite + schema watcher).

---

## 1️⃣ Start the Environment

`powershell
& .\runs\sandbox\run.ps1

